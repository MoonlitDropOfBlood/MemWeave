{
  "name": "@mem-weave/zcode-plugin",
  "version": "1.0.0",
  "description": "zcode plugin for MemWeave. Writes completed conversation turns back to the MemWeave server on Stop, so the consolidation worker can promote high-signal turns to long-term memories.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "test:stop": "node hooks/stop.mjs < fixtures/stop.json"
  },
  "engines": {
    "node": ">=20"
  }
}
