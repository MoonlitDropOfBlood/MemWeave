{
  "name": "@mem-weave/mcp",
  "version": "0.2.3",
  "description": "MemWeave MCP server: stdio-based Model Context Protocol server exposing 10 memory tools (memory_save, memory_recall, memory_smart_search, memory_expand, memory_graph_query, memory_file_history, memory_sessions, memory_patterns, memory_consolidate, memory_forget) over @mem-weave/server's REST API.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "4.4.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT"
}
